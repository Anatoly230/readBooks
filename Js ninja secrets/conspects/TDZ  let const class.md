# 🧊 TDZ — Temporal Dead Zone

> **TDZ — это период времени, в котором переменная уже существует в лексической среде,  
> но доступ к ней запрещён.**

Ключевое слово — **время**, а не строка кода.

---

## 1️⃣ Почему TDZ вообще существует

TDZ появился **намеренно**, чтобы:

- убрать «магическое» поведение `var`
    
- ловить ошибки раньше
    
- сделать код предсказуемым
    

Сравни:

`console.log(x); // undefined 🤨 var x = 10;`

vs

`console.log(y); // ReferenceError 🔥 let y = 10;`

`undefined` — это _слишком тихая ошибка_.  
TDZ делает её **громкой**.

---

## 2️⃣ Самое важное: TDZ ≠ «не объявлена»

❌ Неправильно:

> переменной нет

✅ Правильно:

> переменная **есть**, но **инициализация ещё не произошла**

Внутри движка:

`y → <uninitialized>`

---

## 3️⃣ Когда начинается и когда заканчивается TDZ

### 📍 Начало TDZ

— **в момент создания лексической среды**

### 📍 Конец TDZ

— **в момент выполнения строки с инициализацией**

---

## 4️⃣ Простейший пример (покадрово)

`{   console.log(a); // ?   let a = 10; }`

### Фаза создания блока:

`a → <uninitialized>`

### Фаза выполнения:

`console.log(a); // ❌ ReferenceError`

TDZ **активна**

`let a = 10; // TDZ заканчивается`

---

## 5️⃣ TDZ есть у `let`, `const`, `class`

`console.log(C); // ❌ class C {}`

Классы — тоже TDZ, хотя выглядят как function declaration.

---

## 6️⃣ TDZ и области видимости (где чаще всего ловят баги)

### Пример 1 — перекрытие имён

`let x = 10;  function foo() {   console.log(x); // ❌ ReferenceError   let x = 20; }`

🧠 Что происходит:

- внутри `foo` **есть свой `x`**
    
- он в TDZ
    
- внешний `x` **не используется**
    

---

### Пример 2 — `typeof` НЕ спасает

`console.log(typeof y); // ❌ ReferenceError let y = 10;`

С `var` было можно, с `let` — нет.

---

## 7️⃣ TDZ внутри параметров функции (тонкий момент)

`function foo(a = b, b = 2) {   console.log(a, b); }`

❌ `b` ещё в TDZ, когда вычисляется `a = b`

✔ Правильно:

`function foo(a = 2, b = a) {}`

---

## 8️⃣ TDZ ≠ hoisting (но связано)

||var|let / const|
|---|---|---|
|Хойстинг|да|да|
|Инициализация|`undefined`|❌|
|TDZ|нет|да|
|Ошибка при доступе|нет|да|

> **let хойстится, но не инициализируется**

---

## 9️⃣ TDZ и замыкания (очень важная связь)

`{   let x = 10;    function foo() {     console.log(x);   } }`

✔ TDZ закончилась до вызова `foo`  
✔ замыкание безопасно

Но:

`{   let x = 10;    function foo() {     console.log(x);   }    foo(); // ok }`

А вот так:

`{   foo(); // ❌ TDZ   let x = 10;    function foo() {     console.log(x);   } }`

`foo` создан раньше, **но замыкание смотрит в TDZ**

---

## 🔑 Главный якорь (очень рекомендую запомнить)

> **TDZ — это защита от использования значения до момента,  
> когда движок гарантирует, что оно существует.**

И ещё один:

> **TDZ — это не про порядок строк,  
> это про порядок инициализации.**