Одним из движущих принципов, положенных в основу разработки js как языка программирования, состоял в простоте его применения. И в этом кроется одна из главных причин, по которой не указываются типы значений, возвращаемых из  функций, параметров функций, переменных и т.д. И как вам должно быть уже известно, код js выполняется построчно и непосредственно.
Но если отложить в сторону простоту применения, то каким образом интерпретатору js удаётся вызвать и исполнить функцию до её объявления, если код выполняется построчно?  Оказывается интерпретатор js немного хитрит., и на самом деле код js выполняется в 2 этапа 
Первый этап активизируется всякий раз, когда создаётся лексическая среда. На данном этапе код не выполняется, а интерпретатор js просматривает и регистрирует все переменные и функции, объявленные в текущей лексической среде. После этого начинается второй этап выполнения кода js. Конкретное поведение зависит от типа переменной (`let, vat,const` объявление функции) и типа среды (глобальной, функции или блока).

Данный процесс происходит следующим образом:

1. Если создаётся среда функции `?` то создаётся идентификатор неявного параметра `arguments` на ряду со всем формальными параметрами функции и значениями их аргументов `:` иначе, этот шаг пропускается
2. Если создаётся глобальная среда `||` среда функции `?` то в текущем коде, не доходя до  тела других функций, просматриваются объявления функций, но не функциональные выражения или стрелочные функции! Для каждого обнаруженного объявления функции создаётся новая функция, которая привязывается к идентификатору в среде по своему имени.`:` Если этот идентификатор уже существует, `?` его значение перезаписывается `:` А если это одна из сред блоков `?` то данный шаг пропускается
3. Текущий код проверяется на наличие  объявления переменных. В средах функций и глобальных функций обнаруживаются все переменные, объявленные с помощью ключевого слова `var` и определённые за пределами других функций, хотя их можно разместить в блоках, а также обнаруживаются все переменные , объявленные с помощью ключевых слов `let` и `const`, непосредственно в текущем блоке. Если же идентификатор каждой обнаруженной переменной не существует в текущей среде, он регистрируется и инициализируется неопределённым значением (`undfined`). Но если идентификатор существует, то он оставляется со своим значением.

![[Pasted image 20260123161952.png]]

## Помощь в вопросе от gpt
>[!inte]+ Мой запрос
>давай для начала ты разбери пофазно, поэтапно, в структурированной форме что делаем интерпретатор, какие этапы и что в этих этапах происходит перед запуском кода, но и сним включительно

Отлично, это **идеальный следующий шаг** 👍  
Давай сделаем то, чего обычно не хватает в книгах: **одну цельную модель**, без прыжков и магии.

Я опишу **что именно делает JS-движок**, **в каком порядке**, **в каких структурах памяти**,  
и мы пройдём путь **от «файл загрузился» → до выполнения конкретной строки кода**.

Без замыканий пока — но так, чтобы они потом стали _неизбежным следствием_, а не трюком.

---

### 🧠 Общая картина (1 экран, запомни это)

JS **НЕ**:

- выполняется построчно с нуля
    
- «натыкается» на код
    

JS **ДА**:

1. Загружает код
    
2. Создаёт **Execution Context**
    
3. Внутри него создаёт **Lexical Environment**
    
4. Делает **фазу создания**
    
5. Потом **фазу выполнения**
    
6. Повторяет это **для каждой функции**
    

---

### 🧩 Основные сущности (без них дальше нельзя)

## 1️⃣ Execution Context (EC)

> «Среда выполнения кода прямо сейчас»

Бывает:

- Global Execution Context
    
- Function Execution Context
    
- (eval — не будем трогать)
    

Каждый EC живёт в **Call Stack**

---

#### 2️⃣ Lexical Environment (LE)

> «Где лежат переменные и функции»

LE состоит из:

- **Environment Record** — хранилище идентификаторов
    
- **Outer Environment Reference** — ссылка наружу
    

---

####  3️⃣ Call Stack

> «Стек вызовов функций»

LIFO:

```
| inner | 
| outer |
| global|
```

---

###  🚀 ШАГ 0. Код загружен

```js
console.log(x);
var x = 10;
function foo() {
   let y = 20;
  console.log(y);
   }  
   foo();
```

Ничего **ещё не выполняется**.

---

### 🟦 ШАГ 1. Создаётся Global Execution Context

Как только движок видит код — он:

✔ создаёт **Global Execution Context**  
✔ кладёт его в **Call Stack**

```
Call Stack:

 | Global EC |
```

---

###  🟨 ШАГ 2. ФАЗА СОЗДАНИЯ (Creation Phase)

🔥 **Самый важный этап для понимания JS**

На этом этапе:

- код **НЕ выполняется**
    
- значения **не вычисляются**
    
- функции **не вызываются**
    

#### 2.1 Создаётся Global Lexical Environment

Он содержит:

- Environment Record
    
- Outer → `null`
    

---

#### 2.2 Обработка объявлений

##### 🔹 `var`

`var x = 10;`

На фазе создания:

`x → undefined`

---

#### 🔹 `function`

`function foo() {}`

На фазе создания:

`foo → <function object>`

⚠️ функция создаётся **целиком**, с телом и ссылкой на окружение

---

##### 🔹 `let / const`

`let y = 20;`

На фазе создания:

`y → <uninitialized>`

❌ доступ запрещён до инициализации  
👉 [[TDZ  let const class | TDZ]]

---

#### 2.3 Итог после фазы создания

```js
Global Lexical Environment: 

{
   x: undefined,
   foo: <function>,
	y: <uninitialized> // если бы был
 }
```

---

### 🟩 ШАГ 3. ФАЗА ВЫПОЛНЕНИЯ (Execution Phase)

Теперь код идёт **строка за строкой**.

---

##### ▶️ Строка 1

`console.log(x);`

`x` существует → `undefined`

✔ вывод: `undefined`

---

##### ▶️ Строка 2

`var x = 10;`

Теперь:

`x = 10`

---

##### ▶️ Строка 3 — функция НЕ вызывается

`function foo() { ... }`

Пропускается — она уже создана

---

##### ▶️ Строка 4

`foo();`

🚨 **Вызов функции**

---

###  🟥 ШАГ 4. Создаётся Function Execution Context

Когда функция вызывается:

1️⃣ создаётся новый EC  
2️⃣ кладётся в Call Stack  
3️⃣ повторяются **ТЕ ЖЕ ФАЗЫ**

```js
Call Stack:

| foo EC    |
| Global EC |
```

---

#### 🟨 Фаза создания (внутри функции)

`function foo() {   let y = 20;   console.log(y); }`

Создаётся Function Lexical Environment:

- параметры (если есть)
    
- `arguments`
    
- `let y` → `<uninitialized>`
    

---

#### 🟩 Фаза выполнения (внутри функции)

`let y = 20;`

Теперь:

`y = 20`

`console.log(y); // 20`

---

### 🟦 ШАГ 5. Завершение функции

Функция закончилась:

- `foo EC` **удаляется из Call Stack**
    
- но **Lexical Environment может остаться**, если:
    
    - на него есть ссылка (замыкание)
        

```
Call Stack:

| Global EC |
```

---

### 🔥 КРИТИЧЕСКИЙ МОСТ К ЗАМЫКАНИЯМ

> **Execution Context умирает  
> Lexical Environment — может жить**

Если:

```js
function outer() {
   let x = 10;
      return function inner() {
           console.log(x);   
           }
            }
```

`inner` уносит с собой:

`[[Environment]] → LE outer`

Именно это и есть **замыкание**.

---

### 🧠 Финальный якорь

Запомни одну фразу — она заменяет 50 страниц теории:

> **JS сначала объявляет, потом исполняет.  
> И делает это каждый раз при вызове функции.**