чем __proto__ отличается от prototype

Коротко:  
**`__proto__` — это “ссылка на прототип конкретного объекта”**,  
**`prototype` — это “шаблон, из которого берутся прототипы для новых объектов”**.

А теперь давай аккуратно и по-честному, без магии 🧠

---

## 1️⃣ `[[Prototype]]` — базовое понятие (важно!)

В спецификации есть **внутреннее скрытое свойство** объекта:

`[[Prototype]]`

- его **нет в коде**
    
- оно указывает на **другой объект**
    
- по этой цепочке JS ищет свойства
    

Это и есть **прототипная цепочка**.

---

## 2️⃣ `__proto__` — геттер к `[[Prototype]]`

`obj.__proto__ === Object.getPrototypeOf(obj) // true`

👉 `__proto__` — **не изначальное свойство языка**, а **аксессор**, добавленный в `Object.prototype`.

Что он делает:

- **читает** `[[Prototype]]`
    
- **может менять** `obj` → `[[Prototype]]`
    

`const obj = {}; obj.__proto__ === Object.prototype; // true`

⚠️ Важно:

- считается **устаревшим для логики**
    
- оставлен для совместимости
    
- в проде лучше:
    

`Object.getPrototypeOf(obj) Object.setPrototypeOf(obj, proto)`

---

## 3️⃣ `prototype` — свойство функций-конструкторов

`prototype` **существует ТОЛЬКО у функций** (и классов):

`function Ninja() {} Ninja.prototype`

❗ У обычных объектов **нет `prototype`**:

`({}).prototype // undefined`

---

## 4️⃣ Как они связаны на практике

`function Ninja() {   this.weapon = 'katana'; }  const ninja = new Ninja();`

Происходит следующее:

1. Создаётся пустой объект `{}`
    
2. Его `[[Prototype]]` устанавливается в `Ninja.prototype`
    
3. `this` внутри конструктора = этот объект
    

Результат:

`ninja.__proto__ === Ninja.prototype // true`

📌 Вот ключевая формула:

> **`obj.__proto__ === Constructor.prototype`**

---

## 5️⃣ Визуальная схема

`ninja   |   └── [[Prototype]] → Ninja.prototype                          |                          └── [[Prototype]] → Object.prototype                                                   |                                                   └── null`

---

## 6️⃣ Частые ловушки ⚠️

### ❌ Путают направление связи

`Ninja.__proto__ === ninja.prototype // ❌`

Правильно:

`ninja.__proto__ === Ninja.prototype // ✅`

---

### ❌ Думают, что `prototype` участвует в поиске свойств

Нет.

JS **никогда** не смотрит в `Constructor.prototype` напрямую.  
Он смотрит **только в `[[Prototype]]` объекта**.

---

## 7️⃣ Таблица для фиксации

|Вопрос|`__proto__`|`prototype`|
|---|---|---|
|Где находится|у объектов|у функций|
|Что хранит|ссылку на прототип|объект для будущих экземпляров|
|Участвует в поиске свойств|косвенно (через [[Prototype]])|нет|
|Можно ли у `{}`|да|нет|

---

## 8️⃣ Короткая ментальная модель (очень полезно)

> 🔹 `prototype` — **куда будут ссылаться будущие объекты**  
> 🔹 `__proto__` — **куда ссылается конкретный объект сейчас**